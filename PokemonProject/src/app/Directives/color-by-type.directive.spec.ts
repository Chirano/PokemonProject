import { ColorByTypeDirective } from './color-by-type.directive';

describe('ColorByTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorByTypeDirective();
    expect(directive).toBeTruthy();
  });
});
