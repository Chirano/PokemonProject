import { EvolutionLevelPipe } from './evolution-level.pipe';

describe('EvolutionLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new EvolutionLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
